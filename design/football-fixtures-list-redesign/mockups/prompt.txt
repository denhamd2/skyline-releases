Edit this exact Android app screenshot (Sky-style IPTV app "Home" screen, dark theme, David's profile selected). Keep everything currently visible pixel-faithful and unchanged: the "sky go" wordmark header (blue "sky" + white "go") with search and account icons top-right, the "A Dark Song" hero card with "Newly added" and a blue "Watch now" pill button, the "Continue Watching" section with its progress-bar card, the "Who's watching?" label and its row of four pill chips (David selected in solid action-blue #0B69F5, Anne/Ava/Sophie unselected with dark navy outlined style), the "David's channels" section header, and its pinned channel card ("MUTV" / "Man Utd Classics"). Do not restyle, recolor, or move any of that existing content.

Insert a new "Football" section directly after the "MUTV" pinned-channel card and before the next section that currently follows it. The new section, matching the exact same bold white Manrope-style header typography as "Continue Watching"/"David's channels" above it:

1. A section title "Football" in bold white extra-bold type, same weight as the other section headers.
2. A smaller light blue-grey sub-label "Man Utd next" (semibold).
3. Below it, one full-width spotlight card with rounded 22dp corners and a subtle indigo-to-navy gradient background (deep blue #0C1B87 blending into dark navy): a small rounded competition pill badge top-left reading "Premier League", a centered row with a team crest icon on the left, "Manchester United" team name, a small grey "v", "Opponent FC" team name, and a team crest icon on the right, then a small clock icon with light grey kickoff time text "Sat, KO 15:00".
4. A clearly visible empty vertical gap below the spotlight card (about 16dp).
5. A second small light blue-grey sub-label "This round" (same semibold style as "Man Utd next").
6. Below that, a vertical stack of three more fixture cards — NOT a horizontal carousel, no side-scrolling — each full width with side margins matching the rest of the screen, stacked top to bottom with visible even gaps between them (about 12dp). Each card has a flat dark navy-blue-grey background (#0E1520), rounded 16dp corners, the same competition pill badge, a mirrored team-crest/team-name/"v"/team-name/team-crest row, a kickoff time or live score row, and a row of small outlined action-blue pill chips with a small play-arrow icon reading channel names like "Sky Sports Football" below the team row.

After this new Football section, keep whatever section originally followed the pinned channel card (the "Sky Sports Football" category rail with its channel cards) exactly as it already appears, unchanged, immediately below the new Football section.

Finally, at the very bottom of the image, add one bottom navigation bar (shown once, not repeated) matching this Android app's real navigation: a black/near-black bar with exactly five items in this order — a house icon labeled "Home" (this one selected: action-blue #0B69F5 icon and label), a TV icon labeled "Live" (grey, unselected), a clapperboard icon labeled "Films" (grey, unselected), a TV icon labeled "Series" (grey, unselected), and a list icon labeled "TV Guide" (grey, unselected). No pill or highlight shape behind the selected icon — only the color change indicates selection. Small semibold sans-serif labels under each icon.

Overall aesthetic throughout: premium dark streaming app, Sky Go inspired, bold rounded typography, action-blue (#0B69F5) accents, generous rounded corners, clean 8-point grid spacing, no clutter — matching the existing screenshot's exact style, not inventing a new one.
